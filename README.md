# ASP_SmartBeer_GreenHouse

### Node-RED

#### To install

- `cd node-red`
- `npm install`

#### To run

- `cd node-red`
- `npm start`

### Microcontroller

After cloning the repo use call `git submodule update --init --recursive`.
